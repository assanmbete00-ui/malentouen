import type { SxProps, Theme } from "@mui/material";

const container: SxProps<Theme> = {
  display: "flex",
  alignItems: "center",
  flexWrap: "wrap",
  gap: 2,
  mt: 5,
  justifyContent: "flex-start",
  width: "100%",
};

export default {
  container,
};
