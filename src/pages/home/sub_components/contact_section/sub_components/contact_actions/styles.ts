import type { SxProps, Theme } from "@mui/material";

const container: SxProps<Theme> = {
  display: "flex",
  justifyContent: "flex-start",

  mt: {
    xs: 4,
    md: 5,
  },
};

export default {
  container,
};